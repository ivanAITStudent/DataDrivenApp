using System;

namespace SessionManager
{
    /* The Session Manager manages the session
     * it manages the state of the session
     * it manages the requests and presentation models 
     * it converses with application 
     * it mainatins user session info (user id, username, user type, logged in or not, )
     * 
     */
    public class Class1
    {
    }
}
